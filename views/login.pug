extends index

block variables
  - var containerClassName='container-login'

block content
  section.login
    form.login__form()
      h3.login__heading Log In
      .login__email-wrapper
        label.label.label__email(for='email')
          | Email
        input#email.input.input__email(type='email' name='email' autocomplete='email' required)
      .login__password-wrapper
        label.label.label__password(for='password') Password
        input#password.input.input__password(type='password' name='password' autocomplete='current-password' required)
      button.btn.login__btn(type='submit')
        | Log In
      a.forgot-password(href='/reset-password')
        | Forgot Password?